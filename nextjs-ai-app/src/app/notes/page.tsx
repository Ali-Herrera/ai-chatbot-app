import { Metadata } from "next";

export const metadata: Metadata = {
  title: "AI App - Notes",
};

export default function NotesPage() {
  return (
    <div>
      <h1>Notes Page</h1>
    </div>
  );
}
